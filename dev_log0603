# 2026-06-03 开发日志

## 新增模板自动匹配功能

### 需求
用户选择输入文件后，程序自动根据文件名前缀编号匹配同目录下的模板文件。

### 实现
- 修改 `gui/main_window.py`
- 新增 `_auto_match_template()` 方法
  - 从输入文件名提取数字前缀（如 `01_外校专家聘请审批表_谢晓颖.docx` → `01`）
  - 在程序所在目录扫描包含 `_01_` 的 .docx 模板文件
  - 仅唯一匹配时自动填入模板路径
- 新增 `_on_input_changed()` 回调，仅监听输入文件变化触发自动匹配
- 模板文件变化不触发自动匹配，避免覆盖用户手动选择

### 技术决策
- 使用 `re.match(r'^(\d+)', ...)` 提取文件名前缀数字
- 使用 `getattr(sys, 'frozen', False)` 区分开发环境与打包后 exe 的运行目录
- 匹配规则：文件名同时满足 `.docx` 后缀且包含 `_<编号>_` 字符串
- 零匹配或多匹配均静默跳过，保留用户手动选择的灵活性
